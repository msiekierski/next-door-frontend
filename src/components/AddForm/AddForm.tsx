import React, { FunctionComponent } from "react";

interface OwnProps {
  variant: string;
}

type Props = OwnProps;

const AddForm: FunctionComponent<Props> = (props) => {
  return <></>;
};

export default AddForm;
